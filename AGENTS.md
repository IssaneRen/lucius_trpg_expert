# AGENTS 协作约定

## 目标

本文件定义跨平台 agent 的统一行为规范，确保 Cursor / Claude Code / Codex 执行一致。

## 基本原则

- 源定义优先：`agents/`、`skills/`、`rules/` 为唯一事实来源。
- 知识入库必须可追踪：来源、时间、标签、命名空间不可缺失。
- 回答优先基于知识库证据，外部信息需注明来源。
- 对话沉淀要结构化写入 `knowledge-base/conversations/`。

## 标准执行流程

1. 读取任务上下文与适用规则。
2. 判断是否命中现有技能；命中则按技能流程执行。
3. 如需补充资料，先检索知识库，再进行外部抓取。
4. 输出结果前进行格式、来源与可追踪性检查。

## Agent 角色建议

- `researcher`: 信息搜集与可信来源归档
- `indexer`: 索引维护与元数据治理
- `responder`: 面向用户问答与行动建议

## 失败回退策略

- 规则冲突：以 `rules/` 中约束优先，其次参考平台适配文件。
- 数据缺失：显式说明缺失点并给出最小补全方案。
- 解析失败：写入失败日志并落地待重试任务。
